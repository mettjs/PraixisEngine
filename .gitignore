# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env
.claude
SECURITY_TODO.md

logs/

# Embedded Chroma backend data (VECTOR_BACKEND=chroma)
chroma_data/

# The real model registry: may hold plain API keys as well as ${VAR} refs.
# models.yaml.example is the committed template.
models.yaml

# Local working notes (review companion, not part of the repo)
NOTES.md

.vscode

# Local compose overlay (machine-specific mounts)
docker-compose.override.yml

